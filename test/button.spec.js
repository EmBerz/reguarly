//import { expect } from 'chai';
let expect = require('chai').expect
//let Button = require('../src/components/button.js');

describe('the button component', () => {
  beforeEach(() => {
    console.log('button')
  })
  it('is a button', () =>{
    expect(true).to.equal(true)
  });
})
